<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Task : <%= task.title %></title>

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <!-- Font Awesome -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css"
    />

    <!-- custom css -->
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <div class="app-container">
      <header class="header glass-panel">
        <div style="text-align: left; margin-bottom: 1rem;">
           <a href="/" class="nav-btn nav-btn-all" style="display: inline-flex; align-items: center; gap: 0.5rem;">
            <i class="fa-solid fa-arrow-left"></i> Back to List
          </a>
        </div>
        <h1>EDIT TASK</h1>
        <p style="color: var(--text-secondary); font-size: 1.1rem;">Modifying: <span style="color: var(--text-primary); font-weight: 600;"><%= task.title %></span></p>
      </header>

      <main>
        <section class="form-area glass-panel">
          <form action="/edit/<%= task._id %>" method="post" style="grid-template-columns: 1fr;">
            <div class="input-group">
              <label>Task Title</label>
              <input
                value="<%= task.title %>"
                type="text"
                name="title"
                minlength="3"
                required
                placeholder="Enter task title..."
                class="form-control"
              />
            </div>
            <div class="input-group" style="margin-top: 1rem;">
              <label>Status</label>
              <select name="status" class="form-control">
                <option value="pending" <%= task.status === "pending" ? "selected" : ""  %>>Pending</option>
                <option value="completed"  <%= task.status === "completed" ? "selected" : ""  %>>Completed</option>
              </select>
            </div>
            <button type="submit" class="btn-primary" style="margin-top: 1.5rem; padding: 1rem;">Update Task Details</button>
          </form>
        </section>
      </main>
    </div>
  </body>
</html>
